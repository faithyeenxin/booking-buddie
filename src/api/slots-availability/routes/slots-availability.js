'use strict';

/**
 * slots-availability router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::slots-availability.slots-availability');
