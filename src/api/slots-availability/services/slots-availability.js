'use strict';

/**
 * slots-availability service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::slots-availability.slots-availability');
