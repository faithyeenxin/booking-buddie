'use strict';

/**
 * unavailable-date controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::unavailable-date.unavailable-date');
