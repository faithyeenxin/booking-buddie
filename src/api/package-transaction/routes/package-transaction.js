'use strict';

/**
 * package-transaction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::package-transaction.package-transaction');
