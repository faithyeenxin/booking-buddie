'use strict';

/**
 * package-transaction controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::package-transaction.package-transaction');
